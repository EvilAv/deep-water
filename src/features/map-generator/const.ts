// maybe need to change it from constant to value in mapControl store, but now it seems useless
// so it looks like we can left map in it own slice, since map size is not going to change
export const MAP_SIZE = 8;